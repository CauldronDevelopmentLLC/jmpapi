options:
  google-client-secret: <Replace with Google Secret>
  db-pass: <Replace with DB password>
  http-root: ./http

title: JmpAPI Docs
help: >
  This page describes the JSON over HTTP API endpoints supported by this server.

api:
  docs: !include jmpapi-docs.yaml
  auth: !include jmpapi-auth.yaml
